{
  "name": "key-value-server",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.ts",
  "scripts": {
    "start": "nodemon --ext ts --exec \"tsc || : && node ./dist/index.js\" --watch ./src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Greg Jordan",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "typescript": "^3.7.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.2",
    "nodemon": "^2.0.1",
    "ts-node": "^8.5.4"
  }
}
